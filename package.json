{
  "name": "prompt-forge-trifactor",
  "displayName": "Prompt Forge for VS Code",
  "description": "Build complicated prompts one idea at a time. Every Enter merges an idea into a structured prompt, contradictions surface instead of being guessed away, Polish rewrites it for the model you are sending it to, and Send puts it straight into Claude Code. Works with your own Claude, Gemini, or OpenAI login, or any API key.",
  "version": "0.16.0",
  "publisher": "trifactorscaling",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/trifactorscalingllc/prompt-forge.git"
  },
  "bugs": {
    "url": "https://github.com/trifactorscalingllc/prompt-forge/issues"
  },
  "homepage": "https://github.com/trifactorscalingllc/prompt-forge#readme",
  "engines": {
    "vscode": "^1.94.0",
    "node": ">=20"
  },
  "categories": [
    "AI",
    "Other"
  ],
  "keywords": [
    "prompt",
    "prompt engineering",
    "claude",
    "gemini",
    "openai",
    "chatgpt",
    "llm",
    "copilot"
  ],
  "main": "./extension.js",
  "activationEvents": [
    "onStartupFinished"
  ],
  "extensionKind": [
    "workspace"
  ],
  "capabilities": {
    "untrustedWorkspaces": {
      "supported": "limited",
      "restrictedConfigurations": [
        "promptForge.libraryPath",
        "promptForge.cli.claudePath",
        "promptForge.cli.geminiPath",
        "promptForge.cli.codexPath",
        "promptForge.compatible.baseUrl",
        "promptForge.sourcePath",
        "promptForge.sync.remote"
      ],
      "description": "Prompt Forge runs a vendor CLI as a child process and sends prompts to an endpoint you configure. Paths and endpoints are not read from an untrusted workspace."
    },
    "virtualWorkspaces": {
      "supported": false,
      "description": "Needs a real filesystem for the prompt library and a process host for the vendor CLIs."
    }
  },
  "contributes": {
    "commands": [
      {
        "command": "promptForge.open",
        "title": "Open Prompt Forge",
        "category": "Prompt Forge",
        "icon": "$(tools)"
      },
      {
        "command": "promptForge.newPrompt",
        "title": "New Prompt",
        "category": "Prompt Forge",
        "icon": "$(add)"
      },
      {
        "command": "promptForge.polish",
        "title": "Polish for Target Model",
        "category": "Prompt Forge",
        "icon": "$(sparkle)"
      },
      {
        "command": "promptForge.copy",
        "title": "Copy Final Prompt",
        "category": "Prompt Forge",
        "icon": "$(copy)"
      },
      {
        "command": "promptForge.signIn",
        "title": "Sign In to an Engine",
        "category": "Prompt Forge",
        "icon": "$(sign-in)"
      },
      {
        "command": "promptForge.setApiKey",
        "title": "Set an API Key",
        "category": "Prompt Forge",
        "icon": "$(key)"
      },
      {
        "command": "promptForge.forgetApiKey",
        "title": "Forget an API Key",
        "category": "Prompt Forge",
        "icon": "$(trash)"
      },
      {
        "command": "promptForge.export",
        "title": "Export This Prompt",
        "category": "Prompt Forge",
        "icon": "$(export)"
      },
      {
        "command": "promptForge.newFromTemplate",
        "title": "New Prompt from a Template",
        "category": "Prompt Forge",
        "icon": "$(files)"
      },
      {
        "command": "promptForge.attachProject",
        "title": "Attach a Project Folder",
        "category": "Prompt Forge",
        "icon": "$(folder-opened)"
      },
      {
        "command": "promptForge.toggleRail",
        "title": "Toggle the Prompts List",
        "category": "Prompt Forge",
        "icon": "$(layout-sidebar-left)"
      },
      {
        "command": "promptForge.reload",
        "title": "Reload Prompt Forge Code",
        "category": "Prompt Forge",
        "icon": "$(debug-restart)"
      },
      {
        "command": "promptForge.addIdea",
        "title": "Add an Idea",
        "category": "Prompt Forge",
        "icon": "$(lightbulb)"
      },
      {
        "command": "promptForge.addSelection",
        "title": "Send Selection as an Idea",
        "category": "Prompt Forge",
        "icon": "$(add)"
      },
      {
        "command": "promptForge.sendToClaude",
        "title": "Send This Prompt to Claude Code",
        "category": "Prompt Forge",
        "icon": "$(terminal)"
      },
      {
        "command": "promptForge.attachRemoteProject",
        "title": "Attach a Project over SSH",
        "category": "Prompt Forge",
        "icon": "$(remote)"
      },
      {
        "command": "promptForge.syncNow",
        "title": "Sync the Prompt Library Now",
        "category": "Prompt Forge",
        "icon": "$(sync)"
      },
      {
        "command": "promptForge.setUpSync",
        "title": "Set Up Library Sync",
        "category": "Prompt Forge",
        "icon": "$(repo)"
      },
      {
        "command": "promptForge.live",
        "title": "Share or Join a Live Library",
        "category": "Prompt Forge",
        "icon": "$(live-share)"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "command": "promptForge.addSelection",
          "when": "editorHasSelection",
          "group": "9_promptForge@1"
        }
      ],
      "commandPalette": [
        {
          "command": "promptForge.addSelection",
          "when": "editorHasSelection"
        }
      ]
    },
    "keybindings": [
      {
        "command": "promptForge.addSelection",
        "key": "ctrl+alt+shift+i",
        "mac": "cmd+alt+shift+i",
        "when": "editorTextFocus && editorHasSelection"
      },
      {
        "command": "promptForge.addIdea",
        "key": "ctrl+alt+shift+i",
        "mac": "cmd+alt+shift+i",
        "when": "!editorTextFocus || !editorHasSelection"
      }
    ],
    "customEditors": [
      {
        "viewType": "promptForge.markdown",
        "displayName": "Prompt Forge",
        "selector": [
          {
            "filenamePattern": "*.md"
          }
        ],
        "priority": "option"
      }
    ],
    "configuration": {
      "title": "Prompt Forge",
      "properties": {
        "promptForge.libraryPath": {
          "type": "string",
          "default": "~/.prompt-forge/prompts",
          "scope": "machine-overridable",
          "order": 1,
          "markdownDescription": "Folder holding one `<name>.md` (the prompt) and one `<name>.forge.json` (its history) per prompt. Created on first use. `~` is your home folder."
        },
        "promptForge.engine.provider": {
          "type": "string",
          "enum": [
            "auto",
            "claude",
            "gemini",
            "openai",
            "compatible"
          ],
          "default": "auto",
          "scope": "window",
          "order": 2,
          "enumDescriptions": [
            "The first provider you are signed in to (CLI login first, then a stored API key).",
            "Claude: the `claude` CLI login or an Anthropic API key.",
            "Gemini: the `gemini` CLI login or a Google AI Studio API key.",
            "OpenAI: the `codex` CLI login or an OpenAI API key.",
            "Any OpenAI-compatible endpoint (Ollama, LM Studio, OpenRouter, ...)."
          ],
          "description": "Which account runs the engine. Independent of the target model the prompt is written for."
        },
        "promptForge.engine.mergeModel": {
          "type": "string",
          "default": "auto",
          "scope": "window",
          "order": 3,
          "description": "Model used on every Enter to merge an idea into the document. `auto` picks the provider's fast model."
        },
        "promptForge.engine.polishModel": {
          "type": "string",
          "default": "auto",
          "scope": "window",
          "order": 4,
          "description": "Model used by Polish and by every target-model change. `auto` picks the provider's best model."
        },
        "promptForge.engine.mergeEffort": {
          "type": "string",
          "enum": [
            "low",
            "medium",
            "high",
            "auto"
          ],
          "default": "low",
          "scope": "window",
          "order": 4.1,
          "enumDescriptions": [
            "Fastest. Replaying 33 real merges, low cut the average from 20.6 s to 13.3 s and matched what had been accepted as closely as the default did.",
            "More deliberation on each merge.",
            "The most deliberation a merge gets.",
            "Whatever the provider does when nothing is asked."
          ],
          "markdownDescription": "How hard the model thinks on every merge. Claude, Gemini and OpenAI each receive it in their own terms; a model that does not take it is sent nothing."
        },
        "promptForge.engine.polishEffort": {
          "type": "string",
          "enum": [
            "auto",
            "low",
            "medium",
            "high",
            "xhigh",
            "max"
          ],
          "default": "auto",
          "scope": "window",
          "order": 4.2,
          "markdownDescription": "How hard the model thinks on Polish, a test run and a project brief. `auto` leaves it to the provider."
        },
        "promptForge.engine.mergeOutput": {
          "type": "string",
          "enum": [
            "edits",
            "document"
          ],
          "default": "edits",
          "scope": "window",
          "order": 4.3,
          "enumDescriptions": [
            "The engine returns only the sections it changed; every other section is kept exactly as it is. A merge costs the change, not the whole prompt.",
            "The engine returns the whole document on every merge, as before 0.13."
          ],
          "markdownDescription": "What a merge sends back. With `edits`, a reply that cannot be placed without guessing is asked for again as a whole document, once."
        },
        "promptForge.engine.prewarm": {
          "type": "boolean",
          "default": true,
          "scope": "window",
          "order": 4.4,
          "markdownDescription": "Start the engine process while you type an idea. The Claude CLI spends about six seconds starting; started early, that is over before you press Enter. An unused process is stopped after 90 seconds."
        },
        "promptForge.sync.remote": {
          "type": "string",
          "default": "",
          "scope": "machine",
          "order": 30,
          "markdownDescription": "A git remote the prompt library syncs with, such as a private GitHub repository you own. Prompts are committed on this machine and pulled on the others; when two machines both changed one prompt, their ideas and versions are merged rather than one side winning. Empty turns sync off. Needs `git` and credentials that work without a prompt."
        },
        "promptForge.sync.intervalMinutes": {
          "type": "number",
          "default": 5,
          "minimum": 0,
          "maximum": 1440,
          "scope": "machine",
          "order": 31,
          "description": "How often the library syncs in the background, in minutes. It also syncs shortly after a merge lands and when the window regains focus. 0 syncs only on those events and on demand."
        },
        "promptForge.sync.auto": {
          "type": "boolean",
          "default": true,
          "scope": "machine",
          "order": 32,
          "markdownDescription": "Sync without being asked. Off, the library syncs only with *Prompt Forge: Sync the Prompt Library Now*."
        },
        "promptForge.engine.timeoutSeconds": {
          "type": "number",
          "default": 240,
          "minimum": 30,
          "maximum": 900,
          "scope": "window",
          "order": 5,
          "description": "Hard limit for one engine call."
        },
        "promptForge.engine.recentEntries": {
          "type": "number",
          "default": 12,
          "minimum": 0,
          "maximum": 100,
          "scope": "window",
          "order": 6,
          "description": "How many already-merged ideas the engine is shown as context on each merge."
        },
        "promptForge.layout": {
          "type": "string",
          "enum": [
            "auto",
            "columns",
            "rows"
          ],
          "default": "auto",
          "scope": "window",
          "order": 7,
          "enumDescriptions": [
            "Side by side while there is room, stacked when the panel is narrower than the width below.",
            "Always side by side. Both panels squeeze rather than stack.",
            "Always stacked: Ideas above, Prompt below."
          ],
          "description": "How the Ideas and Prompt panels sit next to each other."
        },
        "promptForge.layoutStackWidth": {
          "type": "number",
          "default": 620,
          "minimum": 0,
          "maximum": 2000,
          "scope": "window",
          "order": 8,
          "markdownDescription": "With `#promptForge.layout#` set to `auto`, stack the panels once the working area is narrower than this many pixels. `0` never stacks."
        },
        "promptForge.layoutSplit": {
          "type": "number",
          "default": 52,
          "minimum": 20,
          "maximum": 80,
          "scope": "window",
          "order": 9,
          "description": "Share of the working area given to Ideas, as a percentage. Drag the divider between the panels to change it."
        },
        "promptForge.railCollapsed": {
          "type": "boolean",
          "default": false,
          "scope": "window",
          "order": 10,
          "description": "Collapse the Prompts list to a strip. Click the chevron on the list, or run Prompt Forge: Toggle the Prompts List."
        },
        "promptForge.suggestions": {
          "type": "boolean",
          "default": true,
          "scope": "window",
          "order": 10.5,
          "markdownDescription": "Show suggestions in the prompt where a section is empty or thin. They are advice only: they live beside the prompt, never inside it, so they are never copied and never saved into the document."
        },
        "promptForge.sendSubmit": {
          "type": "boolean",
          "default": true,
          "scope": "window",
          "order": 10.6,
          "markdownDescription": "Run a prompt as soon as **Send** delivers it to Claude Code. A new or recent conversation opens in a Claude terminal beside the editor with the prompt already sent, and a terminal already running Claude gets the prompt and Enter. Off: the prompt lands in the input box (the Claude Code panel's, when that extension is installed) and pressing Enter is yours."
        },
        "promptForge.projectRoots": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "scope": "machine",
          "order": 11,
          "markdownDescription": "Folders the picker may **list** when you attach a project. Their immediate children that look like projects (a `.git` or a manifest) become the picker's contents. Names and paths only — nothing in these folders is ever read. Empty means the picker offers the open workspace and Browse."
        },
        "promptForge.projectContext": {
          "type": "string",
          "enum": [
            "off",
            "brief",
            "brief+lookup"
          ],
          "enumDescriptions": [
            "Never send project context.",
            "Send the attached project's brief with every merge and polish.",
            "Also search the project for each idea and attach up to three excerpts. Costs a scan on every Enter."
          ],
          "default": "brief",
          "scope": "window",
          "order": 12,
          "description": "Whether an attached project's brief is sent to the engine. Nothing is read until you attach a folder to a prompt."
        },
        "promptForge.projectDefault": {
          "type": "string",
          "enum": [
            "workspace",
            "none"
          ],
          "enumDescriptions": [
            "Attach the folder VS Code has open.",
            "Attach nothing; pick a project per prompt."
          ],
          "default": "none",
          "scope": "window",
          "order": 13,
          "description": "What a brand-new prompt attaches."
        },
        "promptForge.projectMaxFiles": {
          "type": "number",
          "default": 400,
          "minimum": 1,
          "maximum": 5000,
          "scope": "window",
          "order": 14,
          "description": "Hard cap on how many files are considered when building a project brief."
        },
        "promptForge.projectMaxBytes": {
          "type": "number",
          "default": 2000000,
          "minimum": 10000,
          "maximum": 50000000,
          "scope": "window",
          "order": 15,
          "description": "Hard cap on how many bytes are read when building a project brief."
        },
        "promptForge.cli.claudePath": {
          "type": "string",
          "default": "",
          "scope": "machine",
          "order": 10,
          "markdownDescription": "Path to the `claude` binary. Empty finds it on PATH."
        },
        "promptForge.cli.geminiPath": {
          "type": "string",
          "default": "",
          "scope": "machine",
          "order": 11,
          "markdownDescription": "Path to the `gemini` binary. Empty finds it on PATH."
        },
        "promptForge.cli.codexPath": {
          "type": "string",
          "default": "",
          "scope": "machine",
          "order": 12,
          "markdownDescription": "Path to the `codex` binary. Empty finds it on PATH."
        },
        "promptForge.compatible.baseUrl": {
          "type": "string",
          "default": "",
          "scope": "machine",
          "order": 13,
          "markdownDescription": "Base URL of an OpenAI-compatible API, e.g. `http://localhost:11434/v1` (Ollama) or `https://openrouter.ai/api/v1`. The optional key is stored with *Prompt Forge: Set an API Key*."
        },
        "promptForge.docEditor": {
          "type": "string",
          "enum": [
            "forge",
            "office",
            "text"
          ],
          "default": "forge",
          "scope": "window",
          "order": 20,
          "enumDescriptions": [
            "Prompt Forge's own editor: the document formatted, click any part to edit it. Nothing to install.",
            "Office Viewer's WYSIWYG markdown editor, if you have that extension installed.",
            "The plain text editor, markdown source."
          ],
          "description": "How the prompt document opens beside the panel."
        },
        "promptForge.sourcePath": {
          "type": "string",
          "default": "",
          "scope": "machine",
          "order": 90,
          "markdownDescription": "Developer setting. Absolute path to a working copy of the extension. When set, `src/` and `media/` are loaded from there and reloaded on save without restarting the extension host."
        },
        "promptForge.autoReload": {
          "type": "boolean",
          "default": true,
          "scope": "machine",
          "order": 91,
          "markdownDescription": "Developer setting. Reload automatically when a file under `promptForge.sourcePath` changes."
        },
        "promptForge.tokenBudget": {
          "type": "number",
          "default": 0,
          "minimum": 0,
          "scope": "window",
          "order": 9.5,
          "markdownDescription": "Warn once a single prompt has used this many tokens in total. `0` turns it off. Tokens, not money: a CLI login draws on your plan rather than billing per token, so a dollar figure would be wrong for the way most people run this."
        },
        "promptForge.keepVersionBodies": {
          "type": "number",
          "default": 20,
          "minimum": 0,
          "scope": "window",
          "order": 9.6,
          "markdownDescription": "How many recent versions keep a full copy of the document. Older ones keep their record — when, what changed, at whose hand — but not the text, and can no longer be restored. Full copies are most of a prompt’s stored size, which matters if the library is on a synced folder. `0` keeps every copy."
        }
      }
    }
  },
  "scripts": {
    "test": "node --test \"test/*.test.mjs\"",
    "check": "node --check extension.js && node --check src/runtime.js",
    "package": "vsce package --no-dependencies"
  },
  "devDependencies": {
    "@vscode/vsce": "^3.9.2"
  },
  "icon": "media/icon.png",
  "galleryBanner": {
    "color": "#0e0e10",
    "theme": "dark"
  }
}
